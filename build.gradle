plugins {
    id 'java'
    id 'application'
    id 'checkstyle'
    id 'com.github.johnrengelman.shadow' version '5.1.0'
}

checkstyle {
    toolVersion = '8.23'
}

group 'seedu.hustler'
version '0.1.0'

repositories {
    mavenCentral()
}

application {
    // Change this to your main class.
    mainClassName = "seedu.hustler.Hustler"
}

run {
  standardInput = System.in
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter:5.5.0'
}

test {
  useJUnitPlatform()
}

shadowJar {
    archiveBaseName = "duke"
    archiveVersion = "mid-v1.1"
    archiveClassifier = null
    archiveAppendix = null
}
