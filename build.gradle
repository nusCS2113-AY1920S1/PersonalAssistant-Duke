plugins {
    id 'java'
    id 'application'
}

version '0.1.0'

repositories {
    mavenCentral()
}

application {
    // Change this to your main class.
    mainClassName = "ui.Duke"
}

run {
    standardInput = System.in
}