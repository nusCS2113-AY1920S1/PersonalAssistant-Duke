package entertainment.pro.commons.exceptions;

/**
 * Displays an error when a operation fails in the app.
 */
public class Exceptions extends Exception {
    public Exceptions(String message) {
        super(message);
    }
}