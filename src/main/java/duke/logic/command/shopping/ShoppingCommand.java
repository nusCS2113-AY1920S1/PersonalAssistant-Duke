package duke.logic.command.shopping;

import duke.logic.command.Command;

/**
 * A abstract base class for commands that manages the shopping list.
 */
public abstract class ShoppingCommand extends Command {
    public static final String COMMAND_WORD = "shop";
}
