package duke.ui.context;

/**
 * Enum defining the types of contexts that Dr. Duke can take on.
 */
public enum Context {
    HOME,
    PATIENT,
    IMPRESSION,
    INVESTIGATION,
    OBSERVATION,
    PLAN,
    RESULT,
    MEDICINE,
    TREATMENT,
    EVIDENCE,
    SEARCH
}
